/* 2. Crie uma classe Hora que tenha:
a. Três atributos privados e definidos no construtor chamados hora, minutos e
segundos;
b. Métodos públicos para ler hora, minuto e segundo de forma individual;
c. Um método público para retorne a hora no formato “hh:mm:ss”. */

class Hora {
    private horas : number;
    private minutos : number;
    private segundos : number;
    constructor(horas : number, minutos : number, segundos : number) {
        this.horas = horas;
        this.minutos = minutos;
        this.segundos = segundos;
    }
}

function Testar(horas : number, minutos : number, segundos : number) : void {
        while (this.minutos >= 60) && (this.segundos >= 3600) && (this.horas = 60) {
            this.minutos = this.minutos - this.horas;
            this.segundos = this.horas / 3600;
            this.horas = this.segundos * 60;
        }
    }

class Retorno {
    public horas : number;
    public minutos : number;
    public segundos : number;
    constructor(horas : number, minutos : number, segundos : number) {
        this.horas = horas;
        this.minutos = minutos;
        this.segundos = segundos;
    }
}

function Encapsular() : private (array impar[]) : number {
    public this.horas : number {
    public this.minutos : number
}

    console.log(Encapsular);

function Encapsular2() : number {
    return (evens[0]);
}

    console.log(Encapsular2);
